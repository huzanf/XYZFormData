# Block all direct web access to this folder. These are PHP class files
# meant to be loaded by public/*.php only, never requested directly.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
