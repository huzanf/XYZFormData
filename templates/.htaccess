# Block all direct web access to this folder. These are PHP view templates
# meant to be included by public/*.php only, never requested directly.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
